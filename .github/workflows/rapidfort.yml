name: POC - Rapidfort Secure Base Image Scan (Trivy + Cortex)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

env:
  RF_CONTAINER_ENGINE: docker
  RF_ROOT_URL: https://us01.rapidfort.com

jobs:
  scan-rapidfort-image:
    name: Rapidfort Image - Trivy & Cortex Scan
    runs-on: ubuntu-latest
    environment: cortex cli

    steps:
      # 1. Checkout
      - name: Checkout Repository
        uses: actions/checkout@v4

      # 2. Installation CLI Rapidfort
      - name: Install Rapidfort CLI
        run: |
          curl -sSL https://us01.rapidfort.com/cli | bash
          echo "/home/runner/work/cortex-cli/cortex-cli/rapidfort" >> $GITHUB_PATH

      # 3. Debug - Vérification secrets
      - name: Debug - Check RF secrets
        run: |
          echo "RAPIDFORT_USERNAME is set: ${{ secrets.RAPIDFORT_USERNAME != '' }}"
          echo "RAPIDFORT_PASSWORD is set: ${{ secrets.RAPIDFORT_PASSWORD != '' }}"

      # 4. Login Rapidfort via export explicite
      - name: Login to Rapidfort
        run: |
          export RAPIDFORT_USERNAME="${{ secrets.RAPIDFORT_USERNAME }}"
          export RAPIDFORT_PASSWORD="${{ secrets.RAPIDFORT_PASSWORD }}"
          rflogin

      # 5. Pull de l'image Rapidfort
      - name: Pull Rapidfort Image
        run: |
          docker pull quay.io/rfcurated/python:3.12
          docker tag quay.io/rfcurated/python:3.12 rapidfort-python:3.12

      # 6. Scan Trivy
      - name: Run Trivy Scan
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: rapidfort-python:3.12
          format: json
          output: trivy-rapidfort-results.json
          severity: CRITICAL,HIGH,MEDIUM,LOW
          exit-code: '0'

      # 7. Résumé Trivy dans les logs
      - name: Display Trivy Summary
        run: |
          echo "=== TRIVY SCAN SUMMARY - RAPIDFORT ==="
          cat trivy-rapidfort-results.json | jq '
            .Results[] |
            {
              Target: .Target,
              Critical: [.Vulnerabilities[]? | select(.Severity=="CRITICAL")] | length,
              High: [.Vulnerabilities[]? | select(.Severity=="HIGH")] | length,
              Medium: [.Vulnerabilities[]? | select(.Severity=="MEDIUM")] | length,
              Low: [.Vulnerabilities[]? | select(.Severity=="LOW")] | length
            }
          '

      # 8. Upload rapport Trivy
      - name: Upload Trivy Report
        uses: actions/upload-artifact@v4
        with:
          name: trivy-rapidfort-report
          path: trivy-rapidfort-results.json
          retention-days: 30

      # 9. Java 11 requis pour Cortex CLI
      - name: Set up Java 11
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '11'

      # 10. Installation Cortex CLI
      - name: Install Cortex CLI
        run: |
          eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
          export HOMEBREW_NO_REQUIRE_TAP_TRUST=1
          brew tap paloaltonetworks/cortexcli
          brew install cortexcli
          echo "$(brew --prefix)/bin" >> $GITHUB_PATH

      # 11. Scan Cortex
      - name: Run Cortex Image Scan
        env:
          CORTEX_API_BASE_URL: ${{ secrets.CORTEX_API_BASE_URL }}
          CORTEX_API_KEY_ID: ${{ secrets.CORTEX_API_KEY_ID }}
          CORTEX_API_KEY: ${{ secrets.CORTEX_API_KEY }}
        run: |
          echo "=== CORTEX SCAN SUMMARY - RAPIDFORT ==="
          cortexcli image scan rapidfort-python:3.12 --soft-fail 2>&1 | tee cortex-rapidfort-results.txt

      # 12. Upload rapport Cortex
      - name: Upload Cortex Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cortex-rapidfort-report
          path: cortex-rapidfort-results.txt
          if-no-files-found: warn
          retention-days: 30
